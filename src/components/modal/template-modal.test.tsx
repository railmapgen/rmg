import React from 'react';

const mockCallbacks = {
    onClose: jest.fn(),
    onOpenParam: jest.fn(),
};

// TODO: add unit test
describe('Unit tests for TemplateModal component', () => {
    it('Dummy test', () => {
        expect(1 + 1).toBe(2);
    });
});
