import React from 'react';

const mockCallbacks = {
    onClose: jest.fn(),
    onUpdate: jest.fn(),
};

describe('ColourModal', () => {
    // TODO: add unit tests
    it('Dummy test', () => {
        expect(1 + 1).toBe(2);
    });
});
