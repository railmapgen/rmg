import StorageIndexedDB from './storageIndexedDB';

const storage = new StorageIndexedDB();

describe('Unit tests for IndexedDB implementation', () => {
    it('Not figuring out how to test indexedDB', () => {
        // TODO: need to add tests here
        expect(true).toBeTruthy();
    });
});
