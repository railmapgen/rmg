import { removeStation } from './remove-station-action';

//TODO: add unit test for removeStation action
describe('Unit tests for removeStation action', () => {
    it('Dummy test', () => {
        expect(1 + 1).toBe(2);
    });
});
