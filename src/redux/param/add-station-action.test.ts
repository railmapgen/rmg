import { addStation } from './add-station-action';

//TODO: add unit test for addStation action
describe('Unit tests for addStation action', () => {
    it('Dummy test', () => {
        expect(1 + 1).toBe(2);
    });
});
